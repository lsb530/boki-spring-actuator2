spring:
  application:
    name: boki-spring-actuator2
management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
#      show-components: always


info:
  app:
    name: boki-actuator
    company: bk
