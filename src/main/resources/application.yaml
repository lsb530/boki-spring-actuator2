spring:
  application:
    name: boki-spring-actuator2
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
#      show-components: always
